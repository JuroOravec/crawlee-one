# Preview TODO

- **SQLite-backed datasets:** For a more serious implementation, convert Crawlee datasets (JSON files) to SQLite. Store entries in tables and query them via SQL instead of loading everything into memory. This would enable fast filtering, sorting, and pagination at scale without loading full datasets.
